# Database Configuration
# Default is SQLite for instant zero-configuration local development.
DATABASE_URL="file:./dev.db"

# To use PostgreSQL in production, switch the Prisma datasource provider in prisma/schema.prisma to "postgresql" and set:
# DATABASE_URL="postgresql://postgres:password@localhost:5432/cricket_scorer?schema=public"

# Authentication Secret
JWT_SECRET="cricket_super_secret_jwt_key_2024_secure_scoring_app"
NEXTAUTH_URL="http://localhost:3000"
